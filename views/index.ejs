<%- include('layout/header') %>

<div class="container">
    <div class="row my-5">
        <div class="col-lg-12">
            <% if (message) { %>
             <div class="alert alert-dismissible fade show alert-<%= message.type %>"
                role = "alert">
  
                <button class="btn-close" type="button" data-bs-dismiss="alert" aria-label="Close"></button>
                <strong><%= message.message %> </strong>
             </div>
            <% } %>
            <div class="table-responsive">
                <% if(users != '') {%> 
                    <table class="table table-striped my-3">
                        <thead>
                        <tr class="table-dark">
                            <th>ID</th>
                            <th>Profile</th>
                            <th>Name</th>
                            <th>Bio</th>
                            <th>Ratings</th>
                            <th>DOB</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% users.forEach((row, index) => { %>
                            <tr class="align-middle">
                                <td><%= index + 1 %> </td>
                                <td><img src="<%= row.image %>" width="50" class="img-thumbnail"></td>
                                <td><%= row.name %> </td>
                                <td><%= row.bio %> </td>
                                <td><%= row.ratings %><i class="fas fa-star mx-1"> </td>
                                <td><%= row.dob %></td>
                            </tr>
                        <% }) %>
                    </tbody>
                    </table>
                <% }else{ %> 
                    <h1 class="text-center text-secndary mt-5">No users found in Database</h1>                    
                    <%  } %> 
            </div>
        </div>
    </div>
</div>

<!-- <div class="container">
    <div class="row my-5">
        <div class="col-lg-12">
            <% if (message) { %>
             <div class="alert alert-dismissible fade show alert-<%= message.type %>"
                role = "alert">
  
                <button class="btn-close" type="button" data-bs-dismiss="alert" aria-label="Close"></button>
                <strong><%= message.message %> </strong>
             </div>
            <% } %>
            <div class="table-responsive">
                <% if(users != '') {%> 
                    <table class="table table-striped my-3">
                        <thead>
                        <tr class="table-dark">
                            <th>ID</th>
                            <th>Cover</th>
                            <th>Title</th>
                            <th>Description</th>
                            <th>Ratings</th>
                            <th>Date Created</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% users.forEach((row, index) => { %>
                            <tr class="align-middle">
                                <td><%= index + 1 %> </td>
                                <td><img src="<%= row.cover %>" width="50" class="img-thumbnail"></td>
                                <td><%= row.songName %> </td>
                                <td><%= row.songDescription %> </td>
                                <td><%= row.songRatings %><i class="fas fa-star mx-1"> </td>
                                <td><%= row.dateCreated %></td>
                            </tr>
                        <% }) %>
                    </tbody>
                    </table>
                <% }else{ %> 
                    <h1 class="text-center text-secndary mt-5">No songs found in Database</h1>                    
                    <%  } %> 
            </div>
        </div>
    </div>
</div> -->

<%- include('layout/footer')%>